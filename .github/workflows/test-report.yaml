name: Test report
on:
  push:
    branches:
      - main,
      - master

jobs:
  test-report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run tests
        run: dotnet test --collect:"XPlat Code Coverage"